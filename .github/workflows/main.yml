name: Fast APK Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build APK
      uses: ArtemSBulgakov/buildozer-action@v1.5.0
    - uses: actions/upload-artifact@v4
      with:
        name: app
        path: bin/*.apk
